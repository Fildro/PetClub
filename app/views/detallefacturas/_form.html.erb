<%= form_for(@detallefactura) do |f| %>
  <% if @detallefactura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detallefactura.errors.count, "error") %> prohibited this detallefactura from being saved:</h2>

      <ul>
      <% @detallefactura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Factura %><br>
     <%= f.collection_select :factura_id, Factura.all, :id, :id, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :Nombre_Mascota %><br>
     <%= f.collection_select :mascot_id, Mascot.all, :id, :nombrema, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :Servicio %><br>
    <%= f.collection_select :servicio_id, Servicio.all, :id, :tipose, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :Producto %><br>
    <%= f.collection_select :producto_id, Producto.all, :id, :nombrep, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :Cantidad %><br>
    <%= f.text_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :Precio_Venta %><br>
    <%= f.text_field :prevenp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
