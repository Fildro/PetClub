<%= form_for(@factura) do |f| %>
  <% if @factura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factura.errors.count, "error") %> prohibited this factura from being saved:</h2>

      <ul>
      <% @factura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Fecha_Facturas %><br>
    <%= f.datetime_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :Cliente %><br>
    <%= f.collection_select :cliente_id, Cliente.all, :id, :nombrecli, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :Medico %><br>
    <%= f.collection_select :medico_id, Medico.all, :id, :nombrem, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
